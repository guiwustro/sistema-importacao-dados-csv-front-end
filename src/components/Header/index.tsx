import { Container, Title } from "./styles";

const Header = () => {
	return (
		<Container>
			<Title>Plataforma para relacionar clientes e operadores</Title>
		</Container>
	);
};

export default Header;
